" Add markdown snippets to buffers with vimwiki extension
:UltiSnipsAddFiletypes markdown

" Formatting stuff
setlocal textwidth=80 fo=t

" Foldlevel so h1 / h2 headers aren't folded
setlocal foldlevel=2
